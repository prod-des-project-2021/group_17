import Types from '../types';

const initialState = {
    products: null,
    error: undefined,
};

const reducer = (state = initialState, { type, payload }) => {
    switch (type) {
        default:
            return state;
    }
};

export default reducer;
